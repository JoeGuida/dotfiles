return {
    require('plugins.colorschemes'), -- any colorschemes or colorscheme dependencies
    require('plugins.cmp'),          -- any cmp related plugins
    require('plugins.lsp'),          -- any lsp related plugins
    require('plugins.files'),        -- anything to do with files and file types
    require('plugins.view'),         -- anything that changes the visual part of nvim
    require('plugins.editor')        -- anything that affects the editor / functionality
}

